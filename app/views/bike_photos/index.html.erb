<p style="color: green"><%= notice %></p>

<h1>Bike_Photos</h1>

<div id="bike_photo">
  <% @bike_photos.each do |bike_photo| %>
    <%= render bike_photo%>
    <p>
      <%= link_to "Show this bike_photo", bike_photo %>
    </p>
  <% end %>
</div>

<%= link_to "New bike_photos", new_bike_photo_path %>
