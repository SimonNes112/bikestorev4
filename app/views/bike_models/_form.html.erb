<%= form_with(model: bike_model) do |form| %>
  <% if bike_model.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bike_model.errors.count, "error") %> prohibited this bike_brands from being saved:</h2>

      <ul>
        <% bike_model.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :models_name, style: "display: block" %>
    <%= form.text_field :models_name %>
  </div>

  

  <div class="form-group">
    <%= form.label :Bike_brand_id, style: "display: block" %>
    <%= form.select :bike_brand_id, BikeBrand.all.map { |t| [t.id]}, class: "form-control" %>
  </div>


  <div class="form-group">
    <%= form.label :Bike_type_id, style: "display: block" %>
    <%= form.select :bike_type_id, BikeType.all.map { |t| [t.id]}, class: "form-control" %>
    
  </div>


  <div class="form-group">
    <%= form.submit %>
  </div>
<% end %>
